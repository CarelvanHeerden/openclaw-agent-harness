# Install

Install the package through the OpenClaw plugin manager, configure an allowed repository list, authorised host users, storage paths, model credentials, and per-user git credential routing. The package ships no ordinary-user skills; the manifest declares the four control-plane tools directly.

After installation, run the installed-artifact verifier against the exact installed directory.
