# Authentication

Actor and conversation identity come from host-captured tool context. Confirm and merge additionally require an independently verified, one-use host attestation with `provenance=host_verified`. Invocation arguments cannot replace host identity fields.
