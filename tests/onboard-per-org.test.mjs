import test from "node:test";
import assert from "node:assert/strict";
const { evaluatePrReadiness } = await import("../dist/control/readiness.js");
const h="a".repeat(40),route="b".repeat(64);const input=x=>({finalVerdict:"pass",blockingFindings:0,reviewCompleted:true,verificationProbes:{completed:1,required:1,indeterminate:0},candidateSha:h,publication:{sha:h,observedAt:1},pullRequest:{repository:"o/r",baseRef:"main",headSha:h,open:true},expectedRepository:"o/r",expectedBaseRef:"main",requiredCi:{registered:true,requiredChecks:["ci"],successfulChecks:["ci"],sha:h,status:"success"},runtimeEvidence:{status:"pass"},securityEvidence:{status:"pass"},elapsedTimeMs:1,timeLimitMs:10,changedPaths:["src/a"],allowedScope:["src"],excludedScope:[],operationsPerformed:["test"],allowedOperations:["test"],credentialRouteDigest:route,expectedCredentialRouteDigest:route,secretExposure:{detected:false,evidence:"pass"},spendUsd:1,budgetUsd:2,...x});
test("publication is refused if the per-org credential route changes",()=>{assert.equal(evaluatePrReadiness(input({})).ready,true);const r=evaluatePrReadiness(input({credentialRouteDigest:"c".repeat(64)}));assert.equal(r.ready,false);assert.ok(r.failures.includes("credential_route_changed"));});
test("malformed credential route evidence is also refused",()=>{const r=evaluatePrReadiness(input({credentialRouteDigest:"github"}));assert.ok(r.failures.includes("credential_route_changed"));});
