import test from "node:test";
import assert from "node:assert/strict";
const { evaluatePrReadiness } = await import("../dist/control/readiness.js");
const h="a".repeat(40),d="b".repeat(64);const input=(spend,budget)=>({finalVerdict:"pass",blockingFindings:0,reviewCompleted:true,verificationProbes:{completed:1,required:1,indeterminate:0},candidateSha:h,publication:{sha:h,observedAt:1},pullRequest:{repository:"o/r",baseRef:"main",headSha:h,open:true},expectedRepository:"o/r",expectedBaseRef:"main",requiredCi:{registered:true,requiredChecks:["ci"],successfulChecks:["ci"],sha:h,status:"success"},runtimeEvidence:{status:"pass"},securityEvidence:{status:"pass"},elapsedTimeMs:1,timeLimitMs:10,changedPaths:["src/a"],allowedScope:["src"],excludedScope:[],operationsPerformed:["test"],allowedOperations:["test"],credentialRouteDigest:d,expectedCredentialRouteDigest:d,secretExposure:{detected:false,evidence:"pass"},spendUsd:spend,budgetUsd:budget});
test("strict readiness accounts exact spend against the confirmed cap",()=>{assert.equal(evaluatePrReadiness(input(5,5)).ready,true);const over=evaluatePrReadiness(input(5.01,5));assert.equal(over.ready,false);assert.ok(over.failures.includes("spend_exceeded"));});
test("readiness digest binds budget and spend accounting",()=>{const a=evaluatePrReadiness(input(1,5),10),b=evaluatePrReadiness(input(2,5),10);assert.notEqual(a.contentDigest,b.contentDigest);});
