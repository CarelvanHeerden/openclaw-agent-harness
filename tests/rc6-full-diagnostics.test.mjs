import test from "node:test";
import assert from "node:assert/strict";
import { evaluatePrReadiness } from "../dist/control/readiness.js";
const h="a".repeat(40),d="b".repeat(64);const base={finalVerdict:"pass",blockingFindings:0,reviewCompleted:true,verificationProbes:{completed:3,required:3,indeterminate:0},candidateSha:h,publication:{sha:h,observedAt:1},pullRequest:{repository:"o/r",baseRef:"main",headSha:h,open:true},expectedRepository:"o/r",expectedBaseRef:"main",requiredCi:{registered:true,requiredChecks:["lint","test","typecheck"],successfulChecks:["lint","test","typecheck"],sha:h,status:"success"},runtimeEvidence:{status:"pass",sha:h,observedAt:1},securityEvidence:{status:"pass",sha:h,observedAt:1},elapsedTimeMs:1,timeLimitMs:2,changedPaths:["src/a.ts","tests/a.test.ts"],allowedScope:["src/**","tests/**"],excludedScope:[],operationsPerformed:["test"],operationReceipts:[{operation:"test",observedAt:1,source:"test-fixture"}],allowedOperations:["test"],credentialRouteDigest:d,expectedCredentialRouteDigest:d,secretExposure:{detected:false,evidence:"pass"},spendUsd:1,budgetUsd:2};
test("all required CI checks must be present on the candidate SHA",()=>{assert.equal(evaluatePrReadiness(base,2).ready,true);for(const successfulChecks of [["test"],["lint","test","typecheck","extra"],["lint","test","test"]]){const r=evaluatePrReadiness({...base,requiredCi:{...base.requiredCi,successfulChecks}},2);assert.equal(r.ready,false);assert.ok(r.failures.includes("required_ci_not_green"));}});
test("all verification probes must be determinate",()=>{const r=evaluatePrReadiness({...base,verificationProbes:{completed:2,required:3,indeterminate:1}},2);assert.equal(r.ready,false);assert.ok(r.failures.includes("missing_probes"));});
